import React from "react";
import '../styles/header.scss';

const header = () => {
    return (
        <header className="header">
            <h1>List of Notes</h1>
        </header>
    );
};

export default header;